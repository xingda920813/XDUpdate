group 'com.xdandroid'
version '1.0.0'

apply plugin: 'java'
apply plugin: 'scala'

sourceCompatibility = 1.8

sourceSets {
    main {
        scala {
            srcDirs = ['src/main/scala', 'src/main/java']
        }
        java {
            srcDirs = []
        }
    }
}

repositories {
    mavenLocal()
    maven { url "http://114.215.145.40:8081/nexus/content/repositories/bintray" }
    maven { url "http://114.215.145.40:8081/nexus/content/repositories/central" }
    maven {
        name "typesafe-maven-release"
        url "https://repo.typesafe.com/typesafe/maven-releases"
    }
    ivy {
        name "typesafe-ivy-release"
        url "https://repo.typesafe.com/typesafe/ivy-releases"
        layout "ivy"
    }
}

dependencies {
    compile group: 'org.scala-lang', name: 'scala-library', version: '2.11.8'
    compile 'com.aliyun.oss:aliyun-sdk-oss:+'
    compile 'io.reactivex:rxscala_2.11:+'
    compile 'net.dongliu:apk-parser:+'
    compile 'com.google.code.gson:gson:+'
}
